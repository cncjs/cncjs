[data-component="Widgets/SpindleWidget"] {
    .widget-content {
        position: relative;
        padding: 10px;

        .horizontal-mirror {
            transform: scale(-1, 1);
        }
    }
}
