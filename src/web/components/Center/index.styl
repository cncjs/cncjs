.horizontal,
.vertical {
    display: flex;
}

.horizontal {
    justify-content: center;
}

.vertical {
    align-items: center;
}

.stretched {
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
}
