.widget-content {
    position: relative;
    padding: 10px;

    &.hidden {
        display: none;
    }

    .icon {
        display: inline-block;
        height: 14px;
        width: 14px;
        vertical-align: middle;
        margin-top: -2px;

        &.icon-fan {
            background: url("./images/fan.svg") no-repeat 0 0;
        }
    }
}
