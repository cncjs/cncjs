import constants from 'namespace-constants';

// Modal
export const {
  MODAL_NONE,
  MODAL_FEEDER_PAUSED,
  MODAL_FEEDER_WAIT,
  MODAL_SERVER_DISCONNECTED
} = constants('pages/Workspace', [
  'MODAL_NONE',
  'MODAL_FEEDER_PAUSED',
  'MODAL_FEEDER_WAIT',
  'MODAL_SERVER_DISCONNECTED'
]);
