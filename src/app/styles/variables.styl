$navbar-height = 48px; // Tonic UI app header height (measured 48px, no bottom border)
$sidebar-background-color = #222;
$sidebar-width = 60px;
$content-background-color = #fff;
$content-min-width = 720px;
$toggler-width = 50px;
