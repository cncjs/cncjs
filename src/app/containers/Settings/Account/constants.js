import constants from 'namespace-constants';

export const {
    MODAL_CREATE_RECORD,
    MODAL_UPDATE_RECORD
} = constants('containers/settings/account', [
    'MODAL_CREATE_RECORD',
    'MODAL_UPDATE_RECORD'
]);
