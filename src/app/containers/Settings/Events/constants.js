import constants from 'namespace-constants';

export const {
    MODAL_CREATE_RECORD,
    MODAL_UPDATE_RECORD
} = constants('containers/settings/events', [
    'MODAL_CREATE_RECORD',
    'MODAL_UPDATE_RECORD'
]);
