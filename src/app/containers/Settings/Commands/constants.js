import constants from 'namespace-constants';

export const {
    MODAL_CREATE_RECORD,
    MODAL_UPDATE_RECORD
} = constants('containers/settings/commands', [
    'MODAL_CREATE_RECORD',
    'MODAL_UPDATE_RECORD'
]);
